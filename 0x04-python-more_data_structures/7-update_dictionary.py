#!/usr/bin/python3
def update_dictionary(a_dictionary, key, value):
    """ replaces or adds keyvalue in a dictionary given the key to replace or add."""
    a_dictionary[key] = value
    return a_dictionary
